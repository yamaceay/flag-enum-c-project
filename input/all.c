typedef enum restaurant {
  arrived,
  waiting,
  assigned,
  ordered,
  prepared,
  eating,
  paid,
  leaving
} restaurant;

typedef enum tcp_connection {
  syn,
  ack,
  fin
} tcp_connection;

typedef enum font {
  script,
  italic,
  big
} font;

